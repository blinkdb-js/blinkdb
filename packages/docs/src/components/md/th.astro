---
const { class: className, ...attrs } = Astro.props;
---

<th class={`bg-slate-100 dark:bg-slate-800 text-left p-4 border-b border-gray-200 dark:border-gray-700 ${className ?? ''}`} {...attrs}>
  <slot />
</th>