---
layout: "../../../layouts/DocsLayout.astro"
title: "updateMany()"
---

# updateMany()

Updates entities in the table.

An exception is thrown if an entity has not been inserted into the table before,
e.g. if the primary key of an entity was not found.

```ts
// Set the age of alice, bob, and charlie to 20
await updateMany(userTable, [
  { id: "alice-uuid", age: 20 },
  { id: "bob-uuid", age: 20 },
  { id: "charlie-uuid", age: 20 }
]);
```

| Parameter | Description                                                           |
| --------- | --------------------------------------------------------------------- |
| `table`   | The table created by [`createTable()`](/docs/reference/createTable) . |
| `items`    | The items (or for each item, primary key + some properties) to update.    |
